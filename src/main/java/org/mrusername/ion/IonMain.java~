package org.mrusername.ion;

import org.mrusername.ion.core.lang.IonCompiler;
import org.mrusername.ion.core.lang.figure.ClassFigure;
import org.mrusername.ion.core.lang.figure.Figures;

public class IonMain<T> {
    public T x = null;
    public static void main(String[] args) {
        ClassFigure figure = Figures.ofClass(IonMain.class);
        System.out.println(figure);
        System.out.println(new IonCompiler.Builder().build().compile(figure.getPackageName(),figure.parse()));
    }
}
